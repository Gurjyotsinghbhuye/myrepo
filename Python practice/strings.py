name = 'karan'
print(name[0]) #just like an array index it can be called

#name[0] = 'P'  #'str' object does not support item assignment
#print(name[0])

print(name.capitalize()) #First word capital

